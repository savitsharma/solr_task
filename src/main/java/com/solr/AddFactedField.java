package com.solr;import org.apache.solr.client.solrj.SolrClient;import org.apache.solr.client.solrj.SolrQuery;import org.apache.solr.client.solrj.SolrServerException;import org.apache.solr.client.solrj.impl.HttpSolrClient;import org.apache.solr.client.solrj.response.FacetField;import org.apache.solr.client.solrj.response.QueryResponse;import java.io.IOException;import java.util.List;public class AddFactedField {    public    static    void    main(String[] args)            throws SolrServerException, IOException {        // Create a SolrQuery object        SolrQuery query = new SolrQuery();        // Add a facet field for the "category" field        query.addFacetField("category");        // Execute the query and get the response        SolrClient client = new HttpSolrClient.Builder("http://localhost:8983/solr/mycore").build();        QueryResponse response = client.query(query);        // Get the facet counts        FacetField categoryFacet = response.getFacetField("category");        List<FacetField.Count> categoryCounts = categoryFacet.getValues();        // Print the facet counts to the console        for (FacetField.Count count : categoryCounts) {            System.out.println(count.getName() + ": " + count.getCount());        }    }}