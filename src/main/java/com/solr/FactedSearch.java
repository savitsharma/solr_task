package com.solr;import org.apache.solr.client.solrj.SolrClient;import org.apache.solr.client.solrj.SolrQuery;import org.apache.solr.client.solrj.SolrServerException;import org.apache.solr.client.solrj.impl.HttpSolrClient;import org.apache.solr.client.solrj.response.FacetField;import org.apache.solr.client.solrj.response.QueryResponse;import java.io.IOException;public class FactedSearch {    public static void main(String[] args) throws SolrServerException, IOException {        // Create a Solr client        SolrClient solrClient = new HttpSolrClient.Builder("http://localhost:8983/solr/mycore").build();        // Create a Solr query        SolrQuery query = new SolrQuery();        query.setQuery("*:*");        // Add facets to the query        query.addFacetField("category");//        query.addFacetField("price_range");        // Execute the query        QueryResponse response = solrClient.query(query);        // Parse the facet results        FacetField categoryFacet = response.getFacetField("category");//        FacetField priceRangeFacet = response.getFacetField("price_range");        // Print the facet results        System.out.println("Category facet:");        for (FacetField.Count count : categoryFacet.getValues()) {            System.out.println("  " + count.getName() + ": " + count.getCount());        }//        System.out.println("Price range facet:");//        for (FacetField.Count count : priceRangeFacet.getValues()) {//            System.out.println("  " + count.getName() + ": " + count.getCount());//        }    }    }