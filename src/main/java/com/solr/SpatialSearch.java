package com.solr;import org.apache.solr.client.solrj.SolrClient;import org.apache.solr.client.solrj.SolrQuery;import org.apache.solr.client.solrj.impl.HttpSolrClient;import org.apache.solr.client.solrj.response.QueryResponse;public class SpatialSearch {        public static void main(String[] args) {            // Solr URL where Solr instance is running (replace with your Solr URL)            String solrUrl = "http://localhost:8983/solr";            SolrClient solr = new HttpSolrClient.Builder(solrUrl).build();            try {                // Define the query for spatial search                SolrQuery query = new SolrQuery();                query.setQuery("*:*"); // Match all documents                query.set("fq", "{!geofilt sfield=location pt=40.7128,-74.0060 d=10}"); // New York coordinates with a radius of 10 km                // Execute the query                QueryResponse response = solr.query("geospatial_core", query); // Change "geospatial_core" to your core name                // Process and display search results                System.out.println("Search Results:");                response.getResults().forEach(doc -> {                    // Access and display document fields                    System.out.println("Document ID: " + doc.get("id"));                    System.out.println("Document Field(s): " + doc.getFieldNames()); // Adjust as per your field names                });            } catch (Exception e) {                e.printStackTrace();            } finally {                try {                    solr.close();                } catch (Exception e) {                    e.printStackTrace();                }            }        }    }