//package com.solr;////import org.apache.solr.client.solrj.SolrClient;//import org.apache.solr.client.solrj.SolrQuery;//import org.apache.solr.client.solrj.SolrServerException;//import org.apache.solr.client.solrj.impl.HttpSolrClient;//import org.apache.solr.client.solrj.response.QueryResponse;////import java.util.List;////public class factedSearch {//        private SolrClient solrClient;////        public void FacetedSearch(SolrClient solrClient) {//            this.solrClient = solrClient;//        }////        public List<FacetResult> getFacetedResults(String searchQuery, List<String> facetFields) throws SolrServerException {////            // Create a Solr query with faceting enabled.//            SolrQuery solrQuery = new SolrQuery(searchQuery);//            solrQuery.setFacet(true);////            // Add the facet fields to the query.//            for (String facetField : facetFields) {//                solrQuery.addFacetField(facetField);//            }////            // Execute the query and get the results.//            QueryResponse queryResponse = solrClient.query(solrQuery);////            // Get the facet results from the query response.//            List<FacetResult> facetResults = new ArrayList<>();//            for (String facetField : facetFields) {//                FacetResult facetResult = new FacetResult();//                facetResult.setField(facetField);//                facetResult.setCount(queryResponse.getFacetCounts().getFacetField(facetField).getCount());//                facetResults.add(facetResult);//            }////            return facetResults;//        }////        public static void main(String[] args) throws SolrServerException {////            // Create a Solr client.//            SolrClient solrClient = new HttpSolrClient.Builder("http://localhost:8983/solr/my_collection").build();////            // Create a faceted search object.//            FacetedSearch facetedSearch = new FacetedSearch(solrClient);////            // Perform a faceted search.//            List<FacetResult> facetResults = facetedSearch.getFacetedResults("shoes", Arrays.asList("brand", "category"));////            // Print the facet results.//            for (FacetResult facetResult : facetResults) {//                System.out.println(facetResult.getField() + ": " + facetResult.getCount());//            }////            // Close the Solr client.//            solrClient.close();//        }//    }